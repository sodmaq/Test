export enum ServerEnvOptions {
  DEVELOPMENT = "development",
  STAGING = "staging",
  TESTING = "testing",
  PRODUCTION = "production",
}

export enum DebugOptions {
  DEVELOPMENT = "dev",
  STAGING = "stag",
  PRODUCTION = "prod",
}
